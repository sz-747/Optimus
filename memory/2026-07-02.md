## Decisions

- P3 sub-unit order: wire → naming/access → password → command router → CLI. All test-bearing units done, committed separately.
- Unit 4 (command router, commit 5967054): ported CommandRouter.cs + ISocketEffects.cs. `SocketEffects` trait carries only members the router calls (dropped Tree/Ping/CreateNotification 3-arg — add when a non-router consumer needs them). 22 tests.
- Unit 7 (CLI, commit 27bc631): parser + hooks + stdin as `shell/src/cli/`. Kept hand-rolled argv switch, NOT clap — C# original is bespoke, clap would drift behavior. 43 tests (17 parser + 12 hooks + 6 stdin + 8 round-trip).
- Divergence (P2 decision): NotificationId = u64, not Guid. Router parses id as u64; CLI echoes id verbatim; round-trip + parser tests use numeric ids. No C# router test exercised the Guid id path.
- Divergence: report_git_branch bool renders lowercase `true` (Rust Display) in round-trip effect log vs C# `True`. Test adapted.
- Frame builder reuses wire::V2Request Serialize → envelope matches router. params via serde_json Map (keys sort; order-independent, router reads by name).
- CLI lives as a module in shell lib (not separate crate) so round-trip test drives parser + real router in one process. Actual pipe/file I/O binary deferred.
- Shell suite: 237 green (234 + 2 dpapi + 1 pipe_server). New code clippy-clean.
- Unit 6 (job objects, commit 5802570): folded into engine. engine/src/job.rs JobObject::create_for(process, mem_limit) — anonymous job, KILL_ON_JOB_CLOSE always + JOB_OBJECT_LIMIT_PROCESS_MEMORY when limit>0. Enrolled in worker spawn_shell right after publish_child; disposed LAST in teardown (after pty.shutdown) so clean exit wins, KILL only reaps stragglers. EngineOptions.job_memory_limit_bytes (0=uncapped, host wires 2× budget later). Added Win32_System_JobObjects. Runnable check: assigns_child_into_job spawns real child + IsProcessInJob (passes, not build-only).
- Unit 5 (pipe server, commit 073eef6): ipc/{peer,dpapi,pipe_server}.rs. Raw CreateNamedPipeW; owner-SID protected DACL via SDDL `O:{sid}D:P(A;;FA;;;{sid})` (ConvertStringSecurityDescriptorToSecurityDescriptorW) instead of manual ACL build — same semantics as C# PipeSecurity, far less FFI. Blocking I/O, 16-slot semaphore (sync_channel tokens), 1 thread/client. Peer-SID check fail-closed for non-AllowAll. std::fs::File wraps the raw HANDLE for buffered line I/O (owns+closes it). stop() self-connects to wake blocked ConnectNamedPipe. DispatchFn = Arc<dyn Fn(&str)->Option<String>+Send+Sync>. Runnable checks: DPAPI protect/unprotect round-trip + entropy-mismatch-fails-closed; full pipe transport round-trip on a real pipe (both headless, no GUI).
- windows-crate 0.62 gotchas found: BOOL is windows::core::BOOL (not Foundation); OpenProcessToken in Win32::System::Threading (not Security); PIPE_ACCESS_DUPLEX in Storage::FileSystem (not Pipes); PSID in Win32::Security. Rust-2021 disjoint capture grabs `SendHandle.0` (HANDLE, !Send) — rebind whole wrapper inside closure. `unsafe { let _ = f() };` won't parse in stmt position — use `let _ = unsafe { f() };`.

- Main wiring (commit 17198a0): pipe server started in Tauri `setup` (not builder body — so a secondary instance the single-instance plugin rejects never binds the pipe). start_pipe_server resolves name (env else optimus-stable) + mode, PipeServer stored in managed state (lives for app, clean stop on drop). Concrete SocketEffects = PipeEffects: auth LIVE (real DPAPI/env PasswordStore via Win32SecretProtector); domain verbs no-op until workspace host (P4/P5). build_dispatch(mode) → Send+Sync router closure, fail-closed AuthState from mode. Runnable check: app_dispatch_answers_capabilities (system.capabilities V2 → ok/v1,v2). Shell 238 green (237 lib + 1 bin).
- Rust gotcha: `Box::new(std::fs::read)` as `Box<dyn Fn(&str)->_>` fails "Fn not general enough" — fn-item pins one lifetime; use `Box::new(|p| std::fs::read(p))` for HRTB. Same trap if you annotate the closure param `|line: &str|` when coercing to `dyn Fn(&str)`.
- V2 method strings are namespaced: `system.capabilities`, `surface.send_text`, `auth.login`, `notification.*` (NOT bare `capabilities`). capabilities result = `{"capabilities":"v1,v2"}` object, not bare string.

- Domain host (commit 3433496): shell/src/host.rs. Domain = WorkspaceManager + NotificationCoordinator (both !Send: Rc / Box<dyn Fn>). Can't Mutex-share with pipe threads → dedicated thread + mpsc channel = C# DispatcherQueue seam. DomainHost{tx: Arc<Mutex<Sender<Box<dyn FnOnce(&mut Domain)+Send>>>>} (Mutex only to make !Sync mpsc Sender Sync for the Arc<dyn Fn+Send+Sync> dispatch closure). run()=RunOnDispatcher fire-forget, query<T:Default>()=InvokeOnDispatcher blocks (recv().unwrap_or_default → dead domain thread doesn't crash a client). PipeEffects now holds DomainHost, delegates every verb; auth stays separate (password_store, matches C#). build_dispatch(mode, host) clones host per request into fresh PipeEffects. setup spawns host, manages host + PipeServer.
- LIVE now: notification create (record_notification enqueues + drains against owning workspace snapshot so it lands under the right pane; app_focused=false = record unread+toast, safe default), list/dismiss/mark-read/clear/open, jump_to_unread, set-status/set-progress, report_git_branch/pr/pwd (route by surface owner to sidebar row), focus_surface (selects owning workspace only — view-free half). STILL no-op: send_text/send_key (need SurfaceId→engine registry; engine-surface bridge is P5). log_line/sidebar_state no-op = faithful (C# PipeServerEffects no-ops them too).
- Notification create is testable because WorkspaceManager::new() seeds 1 workspace w/ 1 model surface in a tree — create targeting that surface records. Bin e2e test drives create_for_caller (targets selected focused surface) then list through full pipe→router→PipeEffects→host→coordinator. Shell 246 green (244 lib +7 host, 2 bin).

- SurfaceManager into Domain + focus/send routing (this commit): Domain now owns a SurfaceManager (3rd field). Surface trait gained send_text(&str)/send_key(vk,mods) — the input seam (C# ISurface had NEITHER; send went view→pane, but the Rust Domain has no view plane so the Surface trait IS the seam). focus_surface: select owning workspace (as before) + surfaces.get(id)?.focus_surface() — real workspace select, live-surface focus once backed. Added Domain::send_text/send_key (route to surfaces.get(id), no-op unbacked), try_create_surface (capacity choke point), dispose_surface. PipeEffects send_text/send_key now host.run → Domain (was no-op). Key point: new engine only has send_text(bytes) — key encoding is the frontend's job (xterm.js onData); the OLD C# engine's native optimus_engine_send_key is GONE. So send_key(vk,mods) VK→bytes encoding is owned by the future engine-backed Surface impl.
- Domain::new() uses NoSurfaceFactory (refuses spawns) — the real engine-backed factory needs the frontend's per-surface output Channel (dev_spawn_shell), which forces the factory-vs-sink design, so DEFERRED to that unit. Lib stays engine-free (routing is a pure trait seam; EngineSurface lives in the bin later). Domain::with_surface_factory(factory) is the injection ctor (tests + future frontend). Routing proven by 5 host tests w/ a RecordingSurface (send_text/send_key/focus reach it; unbacked id no-op; disposed stops receiving; NoSurfaceFactory refuses). Shell 251 green (249 lib +5 surface routing, 2 bin). Clippy: only the 4 pre-existing lib warnings, new code clean.

- LAST MILE DONE (this commit): engine-backed factory + spawn command + real CapacityModel. main.rs: EngineSurface(Mutex<Option<Engine>>) impls Surface — send_text→engine, send_key→encode_key (VK→VT: Enter/Tab/Esc/Backspace(→DEL 0x7f)/arrows/Home/End/Delete + Ctrl-A..Z→0x01..1A + Alt=meta-ESC; None=drop, ChordModifiers bits Ctrl=1/Alt=4), resize→engine.resize, set_active/focus_surface=NO-OP (WebView owns focus+compositing), shutdown=take() (idempotent). EngineSurfaceFactory: Mutex<HashMap<SurfaceId,StagedSink>> — spawn command stages the invoke's Channel by id (factory.create runs on the DOMAIN thread inside try_create_surface so can't take the Channel via the create() sig → stage-then-create), create pops it, builds+spawns engine. SINGLE owner of live engines now (KTD9) — flat Vec<Engine> DELETED; dev_send_text/dev_resize/dev_spawn_shell all route host.run/query→Domain→SurfaceManager→same engine (frontend + agent unified; per-keystroke domain-thread hop is cheap — engine.send_text is a non-blocking channel send).
- Surface trait GAINED resize(cols,rows) — the engine's 3rd input lever (C# ISurface lacked it: sizing was the renderer's job; here the engine owns the PTY grid + xterm.js fit computes cols/rows). Fakes no-op/record it. Domain::resize + DomainHost::spawn_with(factory,capacity) + Domain::with_surfaces(factory,capacity) added; spawn()=spawn_with(NoSurfaceFactory,None).
- Win32CapacityProvider (bin) via GlobalMemoryStatusEx (added Win32_System_SystemInformation feature): total/avail phys + commit headroom=ullAvailPageFile. STATIC safe-zone cap at startup = crown-jewel hard cap (spawns refuse at cap). ADAPTIVE tier deferred (is_low_memory_signaled=false, subscribe=no-op, measure_private=None — GetPerformanceInfo/PrivateUsage/low-mem notif = plan U3). Job memory cap=0 (uncapped, KILL_ON_JOB_CLOSE still ties lifetime) — a hard 2×budget per-proc ceiling would kill a legit heavy build; count-based safe zone is the real guarantee. Wire per-proc ceiling when calibration is trustworthy.
- dev_spawn_shell targets selected_focused_surface() (the seeded pane) — ponytail: ONE live terminal backing the model's seeded surface. Agent notify/send resolves to it via selected_focused_surface (no OPTIMUS_SURFACE_ID env needed for single-pane). Returns SurfaceId.0 (i32); frontend echoes it back unchanged (ui/main.js needs no edit). query returns Option<Result<i32,String>> (Result isn't Default; None=dead domain thread). unstage on refuse/dead (create already removed the sink on the spawn-error path — unstage idempotent).
- Shell 254 green (249 lib + 5 bin: +encode_key VT set, +factory-refuses-without-staged-sink, +Win32 provider reads nonzero RAM). Clippy: only the 4 pre-existing lib warnings; new code clean.

## Open Questions

- P3/P4 exit gate (plan §5, `optimus hooks claude stop` round-trips end-to-end from a shell in a migrated pane) — WIRING now COMPLETE end to end (transport + domain verbs + live engine-backed surface + capacity gate), but the final confirmation still needs a GUI RUN: no headless way to launch the Tauri window + connect a real external agent here. Manual check to run: `npm run tauri dev` (or the built exe) → a shell spawns in the pane → from inside it run `optimus notify …` / `optimus hooks claude stop` → the toast/sidebar updates; type in the pane → PTY echoes; agent `surface.send_text S1 "…"` reaches the same PTY. This is the one remaining unchecked box.
- NEXT (multi-pane / full agent addressing — was folded into the last mile, now the natural follow-on): (a) dev_spawn_shell currently only backs the seeded surface (one live terminal). To spawn >1, allocate a new tree tab (controller.new_tab) and create THAT surface id. (b) Set OPTIMUS_SURFACE_ID in the child env so an agent in pane N targets pane N (today the agent path resolves to selected_focused_surface — correct only with one pane). Needs engine.spawn_shell/ConPty::spawn to accept an env block (engine surgery — separate unit). (c) Real per-process job memory cap once calibration yields a trustworthy budget (currently 0/uncapped). (d) Adaptive capacity tier (plan U3): Win32CapacityProvider low-memory notification + GetPerformanceInfo + PrivateUsage measurement so the cap tightens under live pressure (currently a static startup cap).
- Deferred inside unit 5 (noted in code): per-connection Password-mode auth-state threading (shared dispatch closure can't hold per-conn login state; PipeEffects derives requires_authentication from mode → Password mode blocks commands, fail-closed); events.stream handler (needs frontend event bus). Peer-SID REJECTION path untested (can't fake a foreign-SID client in-process) — only happy path + fail-closed-on-unresolved covered.
- DONE: domain half (3433496) + SurfaceManager into Domain & focus/send routing (this commit). Engine-backed factory + frontend spawn command is the remaining last mile (see NEXT above). Still lazy: WorkspaceHost.cs routes notifications per-WorkspaceView and aggregates across views — the Rust host uses ONE NotificationCoordinator app-wide (multi-workspace notif aggregation deferred). And per-workspace CreateNotification (V1 3-arg, no surface) isn't in the SocketEffects trait yet (router never calls it).
- Pre-existing clippy warnings (not from P3 units, will trip P6 `-D warnings`): workspace.rs large-variant WorkspaceEvent::Closed(Workspace) [batch B]; password.rs Result<_,()> + 2 complex-type on PasswordStore fields. vt.rs:338 manual_repeat_n.
