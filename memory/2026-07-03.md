## Decisions

- P4 chrome built as 6 vertical slices, each own commit: xterm terminal (e4d1e89), split chrome (01fbd5a), backend split surface (d7893b9), capacity meter (0dbd1d7), pane-visibility governor (00735f4), sidebar (3b970e5), palette (93f6c8e), toasts (34f7343), contentless fix (7331fc7).
- Split tree drives frontend via command-returns-TreeView, NO event bus — structural ops are the only tree mutators, so each mutating command returns the fresh layout. Frontend re-renders from the return value.
- Pane rects reuse tested backend `compute_leaf_rects` (normalized [0,1]); JS never reconciles a tree. Panes keyed by SurfaceId, moved not recreated (scrollback survives split).
- Pane-visibility manager: WebGL attached only to on-screen panes, budget 12 (WebView2 caps ~16 live contexts). Off-screen panes detachWebgl → DOM renderer; buffer independent of renderer so no history loss.
- Capacity meter DTO mirrors ported CapacityIndicatorViewModel (parity tests). Domain stores capacity Arc to expose capacity_view().
- Toast delivery: Domain holds `Option<Box<dyn Fn(Vec<ToastView>)>>` push sink, installed from a Send Tauri Channel via host.run — keeps the lib tauri-free. record_notification drains + pushes show_toast survivors. Push, not poll.
- Terminal disposal made EXPLICIT (close ops return freed surface ids; frontend reaps exactly those). render() no longer disposes by absence from the selected workspace's tree — that killed other workspaces' terminals on switch.
- Never-contentless invariant enforced: close_surface re-seeds on SplitTreeEvent::Emptied; close_workspace flags the Created replacement. CloseReport/CloseResult carry `reseeded`; frontend backs the fresh surface via spawn.
- fmt discipline: rustfmt only the 3-4 files I touch (never `cargo fmt` — it churns 11 pre-existing unformatted P3 files). Left those as status quo — UNTIL the CI gate needed a clean tree (see P6).

## P5 crash-safety (commit 6221a23)

- C4 panic black box: process-wide panic hook (shell/src/panic_log.rs) installed first line of main, appends thread+location+payload+backtrace to %LOCALAPPDATA%\optimus\logs\panic.log, chains to prev hook. Best-effort (never re-panics).
- C4 containment: domain-thread job loop wraps each job in catch_unwind(AssertUnwindSafe) — one panicking command no longer kills the domain (all workspaces). panic=unwind confirmed in release profile, so this + the slot guard actually work.
- C5 slot-leak: pipe client thread returns its concurrency slot via SlotGuard (Drop) instead of a bare send — a panic in dispatch reclaims the slot on unwind instead of leaking it toward max_clients starvation.
- C1 clean close: on WindowEvent::CloseRequested → stop pipe server → host.query(dispose_all_surfaces) (blocking, ordered ClosePseudoConsole→join) before exit.
- Verified pre-existing: C2/C3 (engine ordered teardown + KILL_ON_JOB_CLOSE), C5 malformed-frame (router returns Some(parse_error) for all garbage → connection survives), C8 (terminal.js attachWebgl try/catch + onContextLoss → DOM fallback). C7 satisfied by Tauri default UDF (%LOCALAPPDATA%, writable).
- Tests +2 (266 lib): a_panicking_job_does_not_kill_the_domain_thread, a_panicking_dispatch_releases_the_slot.

## P6 packaging + CI (commits 1254cf5, 59879dc, 418db6c)

- tauri.conf.json already complete: productName/identifier(io.petrex.optimus=AUMID)/icons/NSIS currentUser/webviewInstallMode downloadBootstrapper. windows_subsystem=windows set.
- CI (.github/workflows/ci.yml) NEW — windows-latest: npm ci → npm run build → fmt --check → clippy -D warnings → cargo test --workspace → npm run tauri -- build → upload NSIS artifact. Frontend builds before Rust gates (tauri-build reads frontendDist).
- To make clippy -D warnings honest: 3 deliberate lints marked #[allow]+reason (large_enum_variant WorkspaceEvent, result_unit_err SecretProtector::unprotect, type_complexity PasswordStore), 1 real fix (iter::repeat_n in vt.rs test). Then cargo fmt --all across the whole tree (fmt-check gate needs a clean tree — abandoned the "leave 11 files" discipline for this).
- tauri CLI resolves shell/tauri.conf.json from repo root (verified via `tauri info`) → invocation is `npm run tauri -- build`. build/build.ps1 retargeted from C#/dotnet to a thin wrapper over that.

## Browser mock harness + cap-refusal UX

- Built ui/mock/tauri-mock.js: stateful fake window.__TAURI__.core (invoke+Channel). Real JS split-tree mirrors backend compute_leaf_rects/collect_dividers geometry, so split/close/focus/zoom/divider reshape layout for real; panes typeable (local echo); capacity tracks live pane count; sidebar+toasts carry sample data. Only PTY output + cross-process absent. Lets UI be iterated in a plain browser via `npm run dev`. Node self-check ui/mock/tauri-mock.test.mjs guards the tree math.
- Mock injected by ui/vite.config.js plugin (apply:"serve") — dev server only, prod build never sees it (index.html pristine, dist clean). First attempt (inline guard in index.html) broke `vite build`: @vite-ignore didn't exclude + top-level await fails the build target. Lesson: dev-only frontend scaffolding goes in a serve-scoped vite plugin, never in index.html.
- Claude-in-Chrome browser tool UNUSABLE this session — can't load any URL (example.com errored too), no route to the dev server. Visual iteration handed to user (they screenshot, I adjust). Dev server was on :5180.
- DIAGNOSED user report "Ctrl+Shift+D only makes 3 panes not 10": NOT a bug. Each pane = 1 shell, gated by safe-zone governor. cap = (min(availPhys, commitHeadroom) − 2GB OS reserve) / 200MB SEED_BUDGET. 3 panes ⇒ ~2.6GB free at launch. Ledger is clean 1:1 (no leak). Cap is static-at-launch (freeing RAM after won't raise it). SEED_BUDGET=200MB/terminal is deliberately conservative (real shell ~10-40MB); calibration raises it after 3 samples. Split refusal rolls back + throws → was silent except the tiny AT-CAP chip.
- FIX shipped (18bfa24): spawnInto catch now reads capacity view, toasts the cap hint when at_cap. refreshCapacity returns the painted view; toasts.js gained notify() for frontend-origin cards. Did NOT touch the crown-jewel SEED_BUDGET — that's the user's call.

## Open Questions

- PENDING USER DECISION: lower SEED_BUDGET_BYTES (200MB→~64MB, raises first-run cap ~3x, still conservative) vs leave it honest vs rethink calibration (relax cap as RAM frees post-launch). Asked; user was away. Also confirm what the capacity bar read when D stopped (expect "3 / 3" + AT CAP) to rule out a meter-wiring bug in the packaged app.
- P4 exit gate is a MANUAL N×20-pane WebView2 soak (FPS/heap under keyed-reuse + WebGL governor). Can't run headless. Owed before P4 sign-off.
- DESIGN.md/stylelint conformance pass on the new chrome CSS not run (tokens used throughout, but no automated gate).
- Sidebar unread/latest projected as None (per-workspace NotificationCoordinator wiring deferred). Toasts land globally; per-workspace routing + sidebar badges still owed.
- Notifications reach frontend only via the pipe `notify` path; OSC-99 engine→domain notification wiring not yet confirmed live.
- 4 pre-existing clippy warnings untouched (workspace.rs large-variant, password.rs result_unit_err + 2 type_complexity) — later cleanup pass.
- P5/P6 code done. REMAINING = manual VM verification only (headless can't run these): C1 (close 10 shells → exit 0, no WER, no orphan conhost/shell), C7 (read-only UDF → graceful error), C9 (DPI 100/150/200% multi-monitor), C10 (sleep 5min + resume + type). P6 gate: fresh VM → double-click installer → Start menu → GUI launches, no console, toast works, uninstall clean.
- CI `tauri build` step PROVEN locally: `npm run tauri -- build` exit 0 → target/release/bundle/nsis/Optimus_0.1.0_x64-setup.exe (2.1MB) + target/release/optimus-shell.exe (9.6MB). NSIS 3.11 + nsis_tauri_utils downloaded+bundled fine. Packaging works; only the GUI-launch/uninstall behavior is still manual-VM-owed.
- OWED code: Rust CLI is a lib module, not a bundled sidecar binary (no [[bin]] beyond optimus-shell) — externalBin wiring for `optimus.exe` alongside the app. Frontend stylelint/typecheck + DESIGN.md conformance have no config yet.
- P7 (delete C# app/tests/spikes) is GATED on the P5 manual drill matrix passing — cannot start until the user runs the VM drills. tauri-plugin-window-state + --safe-mode deferred (YAGNI: no session restore exists yet to restore/skip).
